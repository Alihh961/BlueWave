{% block body %}



    <div class="header-container">
        <img src="" alt="logo">
        <nav>
            <a href="{{ path('app_home') }}">Home</a>
            <a href="">Accessories</a>

            {% if app.user %}


                <a href="">My orders</a>


                {% if is_granted("ROLE_ADMIN") %}
                    <a href="/admin">Administration</a>
                {% endif %}

                <div class="container">
                    <div class="drop">
                        <input id="toggle" type="checkbox" checked>
                        <h5 class="toto"><span class="currentBalance">{{ app.user.currentBalance }}$</span> <span class="userName">{{ app.user.firstName }} {{ app.user.lastName }}</span><span class="arrow"></span></h5>
                        <ul id="dropList">
                            <li><a href="/admin">Profile</a></li>
                            <li><a href="{{ path('app_logout') }}">logout</a></li>



                        </ul>
                    </div>

                </div>

            {% endif %}


        </nav>

        {% if not app.user %}
{#                        <a href="/profile"><span>{{ app.user.currentBalance }}$</span> {{ app.user.firstName }} {{ app.user.lastName }}#}
{#                        </a>#}
            <a href="{{ path(app_login) }}">Login</a>
            <a href="{{ path(app_register) }}">Register</a>

        {% endif %}
    </div>


{% endblock %}
